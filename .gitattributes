# On-device Core ML weights. GitHub rejects files >100MB, so weight.bin must
# be stored via Git LFS. Without LFS installed, pushing will fail.
ios/VibeCheckNative/ASTClassifier.mlpackage/Data/com.apple.CoreML/weights/*.bin filter=lfs diff=lfs merge=lfs -text
